import { EventsPage } from "@/components/pages/events";

export const metadata = {
  title: "Events | Church",
  description:
    "Discover upcoming church gatherings, worship nights, and community events.",
};

export default function Page() {
  return <EventsPage />;
}